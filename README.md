# Meus Gastos

PWA estático de controle de gastos pessoais, sincronizado via Firestore
usando um código de acesso em vez de login. Veja `LEIA-ME.txt` para as
instruções de publicação e as regras de segurança do Firestore.
